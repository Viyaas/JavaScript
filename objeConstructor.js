function Dog(name,color,legs)
{
    (this.name="Labrador"),(this.color="white"),(this.legs=4);
}

const animal = new Dog();
console.log(animal);

// using constructor in JS 
// and also this keyword has used to invoke the propertiy names
// the new keyword is create instance object in javascript