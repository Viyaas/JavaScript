function phoneticLookup(val) {
    let result = "";
    let lookup = {
      'alpha ': "Adams",
      bravo: "Boston",
      charlie : "Chicago",
      delta: "Denver",
      echo: "Easy",
      foxtrot: "Frank"
    };
  
    return result;
  }

   let answer  =  phoneticLookup.lookup.charlie;
  //  console.log(lookup[val]);

  console.log(answer);