let str = " karma is boomerang what we give they will be come us ";
let Regex = /w./;
let track = Regex.test(str);

console.log(track);
// the ( /statement./ ) after ending dot is used to match the all string is true or false